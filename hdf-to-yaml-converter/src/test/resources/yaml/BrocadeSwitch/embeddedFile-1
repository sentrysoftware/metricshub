BEGIN {FS="[;]"}
$2 ~ /[0-9]+/ {
   if (($2 / 10625) == int($2 / 10625)) { speed = ($2 / 106.25) }
   else if (($2 / 1250) == int($2 / 1250)) { speed = ($2 / 125.0) }
   else { speed =  $2 / 100}
   print "MSHW;" $1 ";" speed ";"
   }