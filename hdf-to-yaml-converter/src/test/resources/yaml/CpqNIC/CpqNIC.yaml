---
extends:
- ../CpqNetwork-header/CpqNetwork-header
constants:
  Collect_source: "${source::monitors.network.collect.sources.source(1)}"
  Discovery_source: "${source::monitors.network.discovery.sources.source(1)}"
connector:
  displayName: HP Insight Management Agent - Network
  platforms: HP ProLiant
  reliesOn: HP Insight Management Agents
  version: 1.0
  information: "This connector discovers enclosures, Ethernet ports and aggregations through the HP Insight Manager Agent."
  detection:
    connectionTypes:
    - remote
    - local
    appliesTo:
    - NT
    - Linux
    - OSF1
    - VMS
    - Solaris
