---
extends:
- "VPLEX-header"
connector:
  displayName: "EMC VPLEX Version 6"
  platforms: "EMC VPLEX"
  reliesOn: "EMC VPLEX REST API Version 6 and Higher"
  version: "1.0"
  detection:
    connectionTypes:
    - "remote"
    appliesTo:
    - "Storage"
    supersedes:
    - "VPLEX5"
    criteria:
      # DETECTION
      # Does a get_next to see if there that the Chassis OID is populated.
    - type: "http"
      method: "GET"
      url: "/vplex/engines"
