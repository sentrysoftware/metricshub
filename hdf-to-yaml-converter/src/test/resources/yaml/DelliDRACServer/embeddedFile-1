BEGIN {FS=";";}
{
    split($3,outputarray,"-");
    cardType = outputarray[1] ;
    split($4,outputarray,".");
    slotName = outputarray[3]

    if (slotName != "")
    {
        print $1 ";" $2 ";" $3 ";" $4 ";" $5 ";" slotName "." cardType
    }
}