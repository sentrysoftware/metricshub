---
connector:
  detection:
    criteria:
    - type: deviceType
      _comment: >
        //
        // DETECTION
        //
        
        // Exclude Windows, because on Windows, SCSI disks are monitored through
        // the WBEM layer
      exclude: [ NT ]
      keep: [ Linux ]