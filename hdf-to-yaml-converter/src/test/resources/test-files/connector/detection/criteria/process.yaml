- name: process
  input: |
    Detection.Criteria(2).Type="Process"
    Detection.Criteria(2).ProcessCommandLine="cimserver"
  expected:
    connector:
      detection:
        criteria:
        - type: process
          commandLine: cimserver
          _comment: ""

- name: test2
  input: |
    Detection.Criteria(1).Type="Process"
    Detection.Criteria(1).ProcessCommandLine="cimserver"

    Detection.Criteria(2).Type="Process"
    Detection.Criteria(2).ProcessCommandLine="MBM[5-9]\.exe"
  expected:
    connector:
      detection:
        criteria:
        - type: process
          commandLine: cimserver
          _comment: ""
        - type: process
          commandLine: MBM[5-9]\.exe
          _comment: ""