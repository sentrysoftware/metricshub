---
connector:
  detection:
    criteria:
    - type: deviceType
      _comments: >
        //
        // DETECTION
        //
        
        // Exclude Windows, because on Windows, SCSI disks are monitored through
        // the WBEM layer
      exclude: [ NT ]