- name: discovery
  input: |
    // First KeepColumns comment
    Enclosure.Discovery.Source(1).Compute(1).Type="KeepColumns"
    Enclosure.Discovery.Source(1).Compute(1).ColumnNumbers="1,2"

    // Second KeepColumns comment
    Enclosure.Discovery.Source(1).Compute(2).Type="KeepColumns"
    Enclosure.Discovery.Source(1).Compute(2).ColumnNumbers="2,3"
  expected:
    monitors:
      enclosure:
        discovery:
          sources:
            source(1):
              computes:
                - _comment: First KeepColumns comment
                  type: keepColumns
                  columnNumbers: 1,2
                - _comment: Second KeepColumns comment
                  type: keepColumns
                  columnNumbers: 2,3
- name: collect
  input: |
    // First KeepColumns comment
    Enclosure.Collect.Source(1).Compute(1).Type="KeepColumns"
    Enclosure.Collect.Source(1).Compute(1).ColumnNumbers="1,2"

    // Second KeepColumns comment
    Enclosure.Collect.Source(1).Compute(2).Type="KeepColumns"
    Enclosure.Collect.Source(1).Compute(2).ColumnNumbers="2,3"
  expected:
    monitors:
      enclosure:
        collect:
          sources:
            source(1):
              computes:
                - _comment: First KeepColumns comment
                  type: keepColumns
                  columnNumbers: 1,2
                - _comment: Second KeepColumns comment
                  type: keepColumns
                  columnNumbers: 2,3