- name: voltageValueTable
  input: |
    // DeviceID;Status;StatusInformation;Voltage
    Voltage.Collect.ValueTable=%Voltage.Collect.Source(1)%
    Voltage.Collect.DeviceID=ValueTable.Column(1)
    Voltage.Collect.Status=ValueTable.Column(2)
    Voltage.Collect.StatusInformation=ValueTable.Column(3)
    Voltage.Collect.Voltage=ValueTable.Column(4)
  expected:
    monitors:
      voltage:
        collect:
          mapping:
            _comment: DeviceID;Status;StatusInformation;Voltage
            legacyTextParameters:
              StatusInformation: $3
            deviceId: $1
            source: ${source::monitors.voltage.collect.sources.source(1)}
            metrics:
              hw.status{hw.type="voltage"}: $2
              hw.voltage: $4