- name: vmValueTable
  input: |
    // DeviceID;Status;PowerShare;PowerConsumption
    VM.Collect.ValueTable=%VM.Collect.Source(1)%
    VM.Collect.DeviceID=ValueTable.Column(1)
    VM.Collect.PowerState=ValueTable.Column(2)
    VM.Collect.PowerShare=ValueTable.Column(3)
    VM.Collect.PowerConsumtion=ValueTable.Column(4)
  expected:
    monitors:
      vm:
        collect:
          _comment: DeviceID;Status;PowerShare;PowerConsumption
          mapping:
            deviceId: $column(1)
            source: $monitors.vm.collect.sources.source(1)$
            metrics:
              hw.vm.power_state: $column(2))
              hw.vm.power_ratio: percent2Ration($column(3))