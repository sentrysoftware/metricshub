extensions:
  hws_agent:
  hws_agent/1:
    extra_args: ["arg1", "arg2"]

service:
  extensions: [hws_agent/1]
  pipelines:
    traces:
      receivers: [nop]
      processors: [nop]
      exporters: [nop]

# Data pipeline is required to load the config.
receivers:
  nop:
processors:
  nop:
exporters:
  nop:
