connector:
  detection:
    appliesTo: [ Linux ]

beforeAll:
  testCompute:
    type: http
    url: /testUrl/

    computes:
      - type: xml2Csv
        recordTag: recordTagTest
        properties: properties,test,here