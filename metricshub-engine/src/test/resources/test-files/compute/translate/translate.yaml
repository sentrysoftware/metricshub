connector:
  detection:
    appliesTo: [ Linux ]

beforeAll:
  testCompute:
    type: http
    url: /testUrl/

    computes:
      - type: translate
        column: 1
        translationTable: ${translation::translationTableTest}