---
connector:
  detection:
    appliesTo: [ linux ]
    criteria:
    - type: wbem
      query: testQuery
      namespace: testNamespace
      expectedResult: testExpectedResult
      errorMessage: testErrorMessage
      forceSerialization: true

monitors:
  host:
    discovery:
      sources:
        source(1):
          type: wbem
          query: SELECT __PATH,Model,EMCSerialNumber FROM EMC_ArrayChassis
          namespace: root/emc
        source(2):
          type: wbem
          query: SELECT Antecedent,Dependent FROM EMC_ComputerSystemPackage
          nameSpace: root/emc
        source(3):
          type: wbem
          query: SELECT Antecedent,Dependent FROM EMC_SystemPackaging
          namespace: root/emc
        source(4):
          type: tableUnion
          tables: 
            - ${source::monitors.host.discovery.sources.source(2)}
            - ${source::monitors.host.discovery.sources.source(3)}
        source(5):
          type: wbem
          query: SELECT __PATH,ElementName,Description,OtherIdentifyingInfo,OperationalStatus FROM EMC_StorageSystem
          namespace: root/emc
        source(6):
          type: tableJoin
          leftTable: ${source::monitors.host.discovery.sources.source(1)}
          rightTable: ${source::monitors.host.discovery.sources.source(4)}
          leftKeyColumn: 1
          rightKeyColumn: 1
          keyType: WBEM
          defaultRightLine: ;;
        source(7):
          type: tableJoin
          leftTable: ${source::monitors.host.discovery.sources.source(6)}
          rightTable: ${source::monitors.host.discovery.sources.source(5)}
          leftKeyColumn: 5
          rightKeyColumn: 1
          keyType: WBEM
          defaultRightLine: ;;;;
      mapping:
        source: ${source::monitors.host.discovery.sources.source(7)}
        attributes:
          id: buildId($column(6))
          parent: ""
          name: buildName(Storage, EMC, $column(2), (, $column(7), ))
          vendor: EMC
          model: $column(2)
          serial_number: $column(3)
          type: Storage
        conditionalCollection:
          hw.status: $column(10)
    collect:
      type: multiInstance
      sources:
        source(2):
          type: copy
          from: ${source::monitors.host.discovery.sources.source(6)}
        source(1):
          type: wbem
          query: SELECT __PATH,OperationalStatus FROM EMC_StorageSystem
          namespace: root/emc
        source(3):
          type: tableJoin
          leftTable: ${source::monitors.host.collect.sources.source(2)}
          rightTable: ${source::monitors.host.collect.sources.source(1)}
          leftKeyColumn: 5
          rightKeyColumn: 1
          keyType: WBEM
          defaultRightLine: ;;;;
      mapping:
        deviceId: $column(1) 
        source: ${source::monitors.host.collect.sources.source(2)}
        metrics:
          hw.status: $column(2)
  enclosure:
    discovery:
      sources:
        source(1):
          type: wbem
          query: SELECT __PATH,Model,EMCSerialNumber FROM EMC_ArrayChassis
          namespace: root/emc
        source(2):
          type: wbem
          query: SELECT Antecedent,Dependent FROM EMC_ComputerSystemPackage
          nameSpace: root/emc
        source(3):
          type: wbem
          query: SELECT Antecedent,Dependent FROM EMC_SystemPackaging
          namespace: root/emc
        source(4):
          type: tableUnion
          tables: 
            - ${source::monitors.enclosure.discovery.sources.source(2)}
            - ${source::monitors.enclosure.discovery.sources.source(3)}
        source(5):
          type: wbem
          query: SELECT __PATH,ElementName,Description,OtherIdentifyingInfo,OperationalStatus FROM EMC_StorageSystem
          namespace: root/emc
        source(6):
          type: tableJoin
          leftTable: ${source::monitors.enclosure.discovery.sources.source(1)}
          rightTable: ${source::monitors.enclosure.discovery.sources.source(4)}
          leftKeyColumn: 1
          rightKeyColumn: 1
          keyType: WBEM
          defaultRightLine: ;;
        source(7):
          type: tableJoin
          leftTable: ${source::monitors.enclosure.discovery.sources.source(6)}
          rightTable: ${source::monitors.enclosure.discovery.sources.source(5)}
          leftKeyColumn: 5
          rightKeyColumn: 1
          keyType: WBEM
          defaultRightLine: ;;;;
      mapping:
        source: ${source::monitors.enclosure.discovery.sources.source(7)}
        attributes:
          id: buildId($column(6))
          parent: ""
          name: buildName(Storage, EMC, $column(2), (, $column(7), ))
          vendor: EMC
          model: $column(2)
          serial_number: $column(3)
          type: Storage
        conditionalCollection:
          hw.status: $column(10)
    collect:
      type: multiInstance
      sources:
        tableJoin:
          type: tableJoin
          leftTable: ${source::monitors.enclosure.discovery.sources.source(6)}
          rightTable: ${source::monitors.enclosure.collect.sources.source(3)}
          leftKeyColumn: 5
          rightKeyColumn: 1
          keyType: WBEM
          defaultRightLine: ;;;;
        myUnionSource:
          type: tableUnion
          tables:
          - ${source::monitors.enclosure.collect.sources.myExternalSource}
          - ${source::monitors.enclosure.collect.sources.source(2)}
          - ${source::monitors.enclosure.collect.sources.source(3)}
        myExternalSource:
          type: copy
          from: ${source::monitors.host.discovery.sources.source(6)}
        source(2):
          type: copy
          from: ${source::monitors.enclosure.discovery.sources.source(6)}
        source(1):
          type: wbem
          query: SELECT __PATH,OperationalStatus FROM EMC_StorageSystem
          namespace: root/emc
        source(3):
          type: tableJoin
          leftTable: ${source::monitors.enclosure.collect.sources.source(2)}
          rightTable: ${source::monitors.enclosure.collect.sources.source(1)}
          leftKeyColumn: 5
          rightKeyColumn: 1
          keyType: WBEM
          defaultRightLine: ;;;;
      mapping:
        deviceId: $column(1) 
        source: ${source::monitors.enclosure.collect.sources.source(2)}
        metrics:
          hw.status: $column(2)
