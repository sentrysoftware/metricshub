---
connector:
  detection:
    appliesTo: [ linux ]
    criteria:
      - type: ucs
        forceSerialization: true
        query: SELECT dn FROM networkElement
        expectedResult: ^networkElement;sys/
