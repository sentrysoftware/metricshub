connector:
  detection:
    appliesTo: [ linux ]
    criteria:
    - type: wbem
      query: testQuery         # Mandatory and cannot be null
      namespace: testNamespace # Mandatory and default value is root/cimv2
      expectedResult: testExpectedResult
      errorMessage: testErrorMessage
      forceSerialization: true