connector:
  detection:
    appliesTo: [ Linux ]

pre:
  testCompute:
    type: http
    url: /testUrl/

    computes:
      - type: awk
        script: scriptTest
        exclude:  excludeTest
        keep: keepTest